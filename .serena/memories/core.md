# Core

- Python package `Z0Z_tools` under `Z0Z_tools/`; tests under `tests/`.
- Public exports are centralized in `Z0Z_tools/__init__.py`.
- Audio helpers use deterministic sample WAVs in `tests/dataSamples/`.
- Read `mem:tech_stack` for packaging/runtime details, `mem:conventions` for style, `mem:suggested_commands` for local commands, and `mem:task_completion` before finishing coding tasks.